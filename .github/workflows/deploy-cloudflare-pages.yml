name: Deploy Cloudflare Pages

on:
  push:
    branches: ["main"]
    paths:
      - "apps/**"
      - "packages/**"
      - "scripts/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - ".node-version"
      - ".npmrc"
      - "turbo.json"
      - ".github/workflows/**"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: "cloudflare-pages-${{ github.ref }}"
  cancel-in-progress: true

jobs:
  verify:
    name: Verify (${{ matrix.name }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: pnpm-policy
            command: pnpm run test:pnpm-policy
          - name: architecture-invariants
            command: pnpm run test:architecture
          - name: training-contracts
            command: pnpm run test:training-contracts
          - name: heavy-load-boundary
            command: pnpm run test:heavy-load-boundary
          - name: media-lifecycle
            command: pnpm run test:media-lifecycle
          - name: training-lifecycle
            command: pnpm run test:training-lifecycle
          - name: training-privacy
            command: pnpm run test:training-privacy
          - name: game-validator-state
            command: pnpm run test:game-validator
          - name: naming
            command: pnpm run test:naming
          - name: pwa
            command: pnpm run test:pwa
          - name: hub-functions
            command: pnpm run test:hub-functions
          - name: gamerunner
            command: pnpm run test:gamerunner
          - name: trainer-entrypoints
            command: pnpm run test:entrypoints
          - name: cloudflare-deploy
            command: pnpm run test:cloudflare-deploy
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.24.0

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run verification
        run: ${{ matrix.command }}

  deploy:
    needs: verify
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: cloudflare-pages
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.24.0

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Discover Pages app environment
        run: node scripts/write-pages-env.mjs >> "$GITHUB_ENV"

      - name: Check runtime asset changes
        id: runtime-assets
        env:
          BEFORE: ${{ github.event.before }}
        run: |
          if [ "$GITHUB_EVENT_NAME" = "workflow_dispatch" ]; then
            BEFORE="$GITHUB_SHA^"
          fi
          if ! git rev-parse --verify "$BEFORE^{commit}" >/dev/null 2>&1 || ! git diff --quiet "$BEFORE" "$GITHUB_SHA" -- scripts/r2-ai-assets.manifest.json scripts/r2-cors.json; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Configure production R2 runtime asset CORS
        if: ${{ github.ref == 'refs/heads/main' && vars.R2_AI_ASSET_BUCKET != '' && steps.runtime-assets.outputs.changed == 'true' }}
        run: pnpm dlx wrangler@4 r2 bucket cors set "$R2_AI_ASSET_BUCKET" --file scripts/r2-cors.json
        env:
          R2_AI_ASSET_BUCKET: ${{ vars.R2_AI_ASSET_BUCKET }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Sync production R2 runtime assets
        if: ${{ github.ref == 'refs/heads/main' && vars.R2_AI_ASSET_BUCKET != '' && steps.runtime-assets.outputs.changed == 'true' }}
        run: pnpm run sync:r2-ai-assets
        env:
          R2_AI_ASSET_BUCKET: ${{ vars.R2_AI_ASSET_BUCKET }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: Build all apps
        run: pnpm run build:cloudflare:only
        env:
          NEXT_PUBLIC_TURNSTILE_SITE_KEY: ${{ vars.TURNSTILE_SITE_KEY }}
          NEXT_PUBLIC_TURNSTILE_AUTH_REQUIRED: ${{ vars.TURNSTILE_REQUIRED }}
          VITE_TURNSTILE_SITE_KEY: ${{ vars.TURNSTILE_SITE_KEY }}
          VITE_TURNSTILE_AUTH_REQUIRED: ${{ vars.TURNSTILE_REQUIRED }}
          VITE_TURNSTILE_RECORDS_REQUIRED: ${{ vars.TURNSTILE_RECORDS_REQUIRED }}
          NEXT_PUBLIC_CF_WEB_ANALYTICS_TOKEN: ${{ vars.CF_WEB_ANALYTICS_TOKEN }}
          VITE_CF_WEB_ANALYTICS_TOKEN: ${{ vars.CF_WEB_ANALYTICS_TOKEN }}
          VITE_AI_ASSET_BASE_URL: ${{ github.ref == 'refs/heads/main' && vars.AI_ASSET_BASE_URL || '' }}

      - name: Ensure and deploy Cloudflare Pages projects
        timeout-minutes: 15
        run: node scripts/deploy-cloudflare-pages.mjs --branch=${{ github.ref_name }} --production-branch=main
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          AUTH_SESSION_SECRET: ${{ secrets.AUTH_SESSION_SECRET }}
          AUTH_STATE_SECRET: ${{ secrets.AUTH_STATE_SECRET }}
          TURNSTILE_SECRET_KEY: ${{ secrets.TURNSTILE_SECRET_KEY }}
          TURNSTILE_SITE_KEY: ${{ vars.TURNSTILE_SITE_KEY }}
          TURNSTILE_REQUIRED: ${{ vars.TURNSTILE_REQUIRED }}
          TURNSTILE_RECORDS_REQUIRED: ${{ vars.TURNSTILE_RECORDS_REQUIRED }}
          CF_WEB_ANALYTICS_TOKEN: ${{ vars.CF_WEB_ANALYTICS_TOKEN }}
          AI_ASSET_BASE_URL: ${{ vars.AI_ASSET_BASE_URL }}
          ASSET_PUBLIC_BASE_URL: ${{ vars.ASSET_PUBLIC_BASE_URL }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
